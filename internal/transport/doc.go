package transport

// Package transport owns context-aware TCP dialing, login, connection pooling,
// deadlines, health checks, and clean/dirty connection lifecycle decisions. It
// intentionally has no domain API.
